version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/usr/src/app
      - ./node_modules:/usr/src/app/node_modules
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
    command: nodemon --watch server.js --watch src --exec node server.js
